{% extends 'layout.twig' %}

{% block title %} <title>Inscription | {{ parent () }}</title> {% endblock %}
{% block css %} <link rel="stylesheet" href="/assets/css/registration.css{{ _self.cssVersionning() }}"> {% endblock %}

{% block content %}
    <header>
        <nav class='nav'>
            <a href="#" class="links"><img id="logo" src="/assets/img/logo/logo.jpg" alt="Logo" height="100"></a>
            <a href="{{ path('login') }}" class="links" id="login">Connexion</a>
        </nav>
    </header>
    <div class="container-fluid" id="registration">
        <h1 class="text-center" id="h1">Inscription</h1>
        {{ form_start(form,{'action': path('registrationPost') }) }}

        {{ form_end(form) }}
    </div>
{% endblock %}
