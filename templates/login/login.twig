{% extends 'layout.twig' %}

{% block title %} <title>Connexion | {{ parent () }}</title> {% endblock %}
{% block css %} <link rel="stylesheet" href="/assets/css/login.css{{ _self.cssVersionning() }}"> {% endblock %}

{% block content %}
    <header>
        <nav class='nav'>
            <a href="#" class="links"><img id="logo" src="/assets/img/logo/logo.jpg" alt="Logo" height="100"></a>
            <a href="{{ path('registrationGet') }}" class="links" id="registration">Inscription</a>
        </nav>
    </header>
    <div class="container-fluid" id="login">
        <h1 class="text-center" id="h1">Connexion</h1>
        {{ form_start(form,{'action': path('login') }) }}

        {{ form_end(form) }}
    </div>
{% endblock %}
